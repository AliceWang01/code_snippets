setup virtual envrionment
https://www.youtube.com/watch?v=Z58KGp1t4Qw

conda activate env_dash
when in the directory, you can just pip install

conda env remove -n env_dash
conda env create -f mydash.yml
versioning is extremely important